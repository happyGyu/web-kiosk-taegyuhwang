import React from 'react';
import styled from 'styled-components';

export default function Cart() {
  return <CartWrapper>hi</CartWrapper>;
}

const CartWrapper = styled.div`
  width: 40%;
  margin-right: 1rem;
`;
